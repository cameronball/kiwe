// Code to be ran when the document is ready
$(document).ready(() => {
	// Remove the far class from the tokens button icon and add the fas class
	$("#tokensButtonIcon").removeClass("far").addClass("fas");
})
